language: d

os:
  - linux
  - osx

d:
  - dmd-2.069.2
  
script:
    - dub run :printdirs --compiler=${DC}
    - if [ `uname` = "Darwin" ]; then dub run :printdirs --config=cocoa -L-framework -LFoundation ; fi

